# Porta do servidor Fastify
PORT=3001
HOST=0.0.0.0

# Diretório de cache dos MP3 (default: ./cache)
# CACHE_DIR=./cache

# Origem permitida pelo CORS (default: * — ideal para uso local)
# CORS_ORIGIN=http://localhost:5173

# Limites de busca/mix
SEARCH_LIMIT=20
MIX_LIMIT=25
